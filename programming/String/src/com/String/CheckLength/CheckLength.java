package com.String.CheckLength;

public class CheckLength {
static String checkLength(int len1,int len2) {
	//check len1 is equal to len 2 or not
	if(len1==len2) {
		return "Both the strings have same length";
	}
	else {
		return "Both the String have different length";
	}
} 
}
