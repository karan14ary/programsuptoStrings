package CityPlanningSimulator;

public class CityPlanningSimulator {
	public static int calculatePlotArea(int sideLength) {
		return sideLength*sideLength;//calculate area of plot and return int
	}

	public static double calculatePlotArea(double sideLength) {
		return sideLength*sideLength;//calculate area of plot and return double
	}
	
}
